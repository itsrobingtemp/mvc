{% extends "base.html.twig" %}

{% block title %}Card Deck{% endblock %}

{% block body %}
    <h1>Draw</h1>
    <p>Här är ett slumpmässigt kort</p>

    <p>Nuvarande kort i kortleken: {{ count }}</p>

    <div class="cards">
      <div class="card {{card[2]}}">
        <div class="card__item">{{ card[1] }}</div>
        <div class="card__item">{{ card[0] }}</div>
        <div class="card__item">{{ card[1] }}</div>
      </div>
    </div>
{% endblock %}